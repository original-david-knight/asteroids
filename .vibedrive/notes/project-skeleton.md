Initialized root Rust crate with exact dependency pins compatible with Rust 1.90. wgpu/winit app opens a borderless fullscreen black Wayland/Hyprland window on the 5120x2160 monitor with FIFO vsync; Esc exits 0 in smoke test. Audio module shape is present with AudioMsg, POD GameSnapshot, 1024-message ringbuf channel that evicts the oldest queued SetParam when full, AtomicF32-backed preallocated voices, and feature-gated cpal placeholder with no stream spawned. Existing docs/probe files preserved unchanged.
