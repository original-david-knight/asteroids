Implemented a disposable wgpu hello-triangle pass using the selected Rgba16Float surface format, an empty pipeline layout, one vertex buffer, and a single draw call after the black clear. Key gotchas: no bind groups are needed for this pass; vertex buffer attributes must match WGSL locations exactly; future passes should continue threading target formats from surface/offscreen configuration.
