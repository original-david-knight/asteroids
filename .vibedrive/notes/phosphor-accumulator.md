Implemented native-resolution Rgba16Float phosphor ping-pong with SDF beam pass, exponential decay blend, Reinhard/gamma composite, and F1/F2/F3 tau controls. Rgba16Float path was selected; no Rgba8Unorm fallback used. Because wgpu cannot sample a render target while writing it, the beam target is copied to a scratch texture before the blend pass samples previous history plus current beam. cargo test passed and timeout 6s cargo run exercised the 5120x2160 Rgba16Float path without validation errors.
