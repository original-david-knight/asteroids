Fixed-step 1/240s ship simulation is wired into interactive play and heavy-input automation with a 4-substep spiral guard, interpolated rendering, H-only hyperspace input, and GameSnapshot tick pushes when an audio sender exists. Unit tests cover the guard, wrap, rotation, thrust accumulation, max velocity, and no-friction inertia. Heavy-input frame p99 was 0.643910ms against the 7.5ms gate.
