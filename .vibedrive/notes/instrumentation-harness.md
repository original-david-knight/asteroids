Headless wgpu harness renders offscreen at the probed native 5120x2160 resolution (ASTEROIDS_HEADLESS_SIZE can override), writes PNG screenshots/frame captures, logs frame/state/xrun files, and keeps no-flags cargo run on the interactive fullscreen path. Verification helpers and verify CLI are installed with synthetic tests, including PNG/WAV/log/state and spectral helpers. Review fix: decay-fit now traces the first-frame peak row so the 60-frame horizontal-sweep gate fits tau=0.069767s with r_squared=0.999964; frame files use frame_00000-style padding and numeric sorting. WAV capture currently writes valid 48 kHz stereo PCM16 silence on a background thread until audio-scaffolding provides real samples; fixed-size ringbuf capture batches are ready for the future callback without allocation. Initial scenarios shipped: idle, horizontal-sweep, static-bright-line, low/high dwell aliases, and gamma-ramp. --seed now initializes a crate-level deterministic RNG hook and records deterministic state-log outcomes for downstream simulation code.
